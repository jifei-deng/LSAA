# LSAA

## Training example

All default arguments and hyperparameters (`common/arguments.py`) are set for the **Find the Treasure (FT)** environment.

**Find the Treasure (FT)**

```
python main.py --alg lsaa --env_name ma_gym:FindTreasure-v0 --max_episodes 50000 --reuse_network True --individual_rewards True
```
